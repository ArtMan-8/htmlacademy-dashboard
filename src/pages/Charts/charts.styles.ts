import { createStyles, makeStyles } from '@material-ui/core/styles';

export default makeStyles(() =>
  createStyles({
    charts: {
      margin: '20px auto',
      maxWidth: 600,
      padding: 20,
    },
    note: {},
  }),
);
